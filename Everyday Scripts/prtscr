#! /bin/bash

# I bound this script to my PrintScreen button
# This script make a screenshot of focused window, and put it into your clipboard

scrot -u /tmp/foo.png
copyq write image/png - < /tmp/foo.png && copyq select 0
rm /tmp/foo.png
